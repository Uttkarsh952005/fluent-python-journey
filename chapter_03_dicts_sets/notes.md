"""
Chapter 3: Notes — Dicts and Sets
===================================
Quick reference notes. Full content coming soon.

## Key Vocabulary

- hash table: data structure mapping keys to values via hash function
- load factor: ratio of filled slots to total slots (Python uses ~2/3)
- hash collision: two keys produce the same hash value
- open addressing: collision resolution by probing nearby slots
- compact dict: Python 3.6+ layout separating index array from entry array
- dict view: a live view of dict keys/values/items (not a snapshot copy)
- frozenset: immutable, hashable set
"""
